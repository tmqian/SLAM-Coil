[project]
name = "slam-coil"
version = "0.1.0"
description = "Field analysis tools for coil configuration for the SLAM lab"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy",
    "pandas",
    "scipy",
    "matplotlib",
]
